_base_: ../stage2_segment_roi_outside.yaml

data:
  manifest: Manifests/segmentation_folds/manifest_seg_fold0.csv
  root: .
  target_size: [512, 512]
  normalization: fixed

train:
  output_dir: runs/current/stage2_segment_roi_outside_fold0
  pretrained: runs/current/stage1_denoise_fold0/best.pth

