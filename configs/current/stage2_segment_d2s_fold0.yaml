_base_: ../stage2_segment_d2s.yaml

data:
  manifest: Manifests/segmentation_folds/manifest_seg_fold0.csv
  root: .
  target_size: [512, 512]
  normalization: fixed

train:
  output_dir: runs/current/stage2_segment_d2s_fold0
  pretrained: runs/current/stage1_denoise_fold0/best.pth

