# Failure cases

No scientific D2 run has been executed locally. Future review must use one
fixed sample list for every arm and include component misses, low-contrast
misses, boundary deletion, residual structure leakage, hallucinated structures,
and location-specific reversals. Samples may not be selected independently per
model.

